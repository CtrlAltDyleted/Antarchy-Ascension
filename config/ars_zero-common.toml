#Blight forest biome (requires TerraBlender). Set to 0 to disable.
[blight_forest]
	#TerraBlender region weight for Blight Forest (vanilla defaults to 10).
	#Requires a full restart and only affects newly generated chunks.
	# Default: 1
	# Range: > 0
	weight = 1
